(encrypt_op(rr;mm) == (rr : NAT & mm : NAT));
(decrypt_op(rr;mm) == (rr : NAT & mm : NAT))
|
(ee : NAT);
(dd : NAT);
(nn : NAT);
(encrypt : POW(NAT*NAT));
(decrypt : POW(NAT*NAT));
(gcd : POW(NAT*NAT*NAT));
(prime : POW(NAT));
(exp : POW(NAT*NAT*NAT))|
(ee , dd , nn)
