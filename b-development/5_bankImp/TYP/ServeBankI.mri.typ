(startServer(ok;?) == (ok : BOOL));
(listenForUser(?;?) == ?)
|
(true);
(given(SCALAR \/ BOOL));
(maxAccounts : NAT);
(accountNumber : POW(NAT));
(account : POW(NAT*NAT));
(outbufsize : NAT);
(no_of_saves : NAT);
(outptr : NAT);
(in : NAT);
(inptr : NAT);
(encrypt : POW(NAT*NAT));
(decrypt : POW(NAT*NAT));
(publicDecrypt_Nvar : NAT)|
(?)
